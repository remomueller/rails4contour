<% @title = 'Items' %>
<div class="page-header">
  <h1>
    <%= @title %>
    <%= link_to "Create Item", new_item_path, class: 'btn btn-mini btn-primary' %>
  </h1>
</div>

<%= form_tag items_path, method: :get, remote: true, id: "items_search", class: 'form-search', data: { object: 'form-load', no_turbolink: true } do %>
  <%= hidden_field_tag :order %>
  <%= text_field_tag 'search', params[:search], class: 'search-query' %>
  <%= submit_tag 'Search', class: 'btn btn-primary' %>
<% end %>

<div id="items"><div class="center"><%= image_tag "contour/ajax-loader.gif" %></div></div>
